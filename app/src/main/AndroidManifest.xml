<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bortolan.iquadriv2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="252ed2b733dd81ce76fe61f583737b98a6b5eaf3" />

        <receiver android:name=".Broadcasts.OnBoot">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_RESTARTED" />

                <data
                    android:path="com.bortolan.iquadriv2"
                    android:scheme="package" />
            </intent-filter>
        </receiver>
        <receiver android:name=".Broadcasts.Notifiche" />

        <activity
            android:name=".Activities.SettingsActivity"
            android:theme="@style/AppTheme.Preferences" />
        <activity
            android:name=".Activities.OrarioActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_orario"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".Activities.ArticleActivity"
            android:label="@string/title_activity_article"
            android:theme="@style/AppTheme" />

        <service
            android:name=".MyFirebaseMessagingService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".Activities.LibriLogin"
            android:launchMode="singleInstance" />
        <activity android:name=".Activities.AddBook"></activity>
    </application>

</manifest>